I"3<p>This file is part of a collection.
Use</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>collections:
  collectionName:
    output: true
</code></pre></div></div>
<p>in your <code class="language-plaintext highlighter-rouge">_config.yml</code> to generate a collection. Entries in the collection must be put in a folder with the same <code class="language-plaintext highlighter-rouge">_collectionName</code> as in the config (don’t forget the underscore).</p>

<p>If you want to use a different title instead of <code class="language-plaintext highlighter-rouge">collectionName</code> you can provide</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    title: "Collections"
</code></pre></div></div>
:ET